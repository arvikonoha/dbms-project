const deleteContact = {
  currentForm: "delete-contact",
  formFields: [],
  contents: ["Phone", "Address"],
  formTitle: "Are you sure about deleting the following content",
}

export default deleteContact